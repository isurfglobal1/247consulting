# Destination address for all site forms (contact, job application, etc.)
# Copied into the built JS bundle at build time by Vite — works on static
# hosting (e.g. Hostinger) since no server is required, only mailto: links.
VITE_CONTACT_EMAIL=executive@247hr.com
